[build]
  publish = "."
  command = ""

[build.environment]
  NODE_ENV = "production"

[dev]
  command = ""

# Forms will be handled by Netlify automatically when deployed

[context.production]
  publish = "."

